"use strict";

iLog("starting drone boot sequence");

var bebop = require("../.");

var drone = bebop.createClient();
iLog("drone client created");

drone.connect(function() {
  
drone.stop();
iLog("land executing");
drone.Piloting.landing();
drone.land();
});

iLog("finished drone boot sequence");

function iLog(vMsg) {
console.log(vMsg);
}